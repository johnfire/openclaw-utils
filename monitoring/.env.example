# Configuration for Session Token Monitor
# Copy to .env and adjust values

# OpenClaw binary path
# Find with: which openclaw
OPENCLAW_PATH="/usr/local/bin/openclaw"

# WhatsApp number to send alerts to
ALERT_NUMBER="+1234567890"

# Token thresholds (in thousands)
WARNING_THRESHOLD=100  # 100k tokens
ALERT_THRESHOLD=125    # 125k tokens
MAX_TOKENS=200         # 200k tokens (default OpenClaw context)

# Alert messages
WARNING_MESSAGE="High token usage detected ({current}k/{max}k). Consider 'new session' command."
ALERT_MESSAGE="CRITICAL: Token usage very high ({current}k/{max}k). Send 'new session' immediately."

# Check interval (seconds) - for script loop mode
CHECK_INTERVAL=900  # 15 minutes

# Log file location
LOG_FILE="/tmp/openclaw_token_monitor.log"